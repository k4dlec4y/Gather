<Window
	x:Class="WPF.Views.UserV.SendBecomeOrganizerRequestView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:WPF.Views.UserV"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Title="SendBecomeOrganizerRequest"
	Width="800"
	Height="460"
	Background="#FF0a0a0a"
	WindowStartupLocation="CenterScreen"
	mc:Ignorable="d">

	<Window.Resources>
		<Style TargetType="TextBlock">
			<Setter Property="Foreground" Value="White" />
			<Setter Property="FontSize" Value="14" />
			<Setter Property="Margin" Value="0,5,0,5" />
		</Style>

		<Style TargetType="Button">
			<Setter Property="Background" Value="#FF7FFFD4" />
			<Setter Property="Foreground" Value="#FF1E1E2F" />
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="Margin" Value="10,5" />
			<Setter Property="Padding" Value="10,5" />
			<Setter Property="BorderBrush" Value="Transparent" />
			<Setter Property="Cursor" Value="Hand" />
			<Setter Property="Effect">
				<Setter.Value>
					<DropShadowEffect
						BlurRadius="8"
						Opacity="0.6"
						ShadowDepth="0"
						Color="#FF7FFFD4" />
				</Setter.Value>
			</Setter>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#FF66E6B8" />
					<Setter Property="Effect">
						<Setter.Value>
							<DropShadowEffect
								BlurRadius="15"
								ShadowDepth="0"
								Color="#FF7FFFD4" />
						</Setter.Value>
					</Setter>
				</Trigger>
			</Style.Triggers>
		</Style>

		<Style TargetType="TextBox">
			<Setter Property="Background" Value="#FF1E1E2F" />
			<Setter Property="Foreground" Value="White" />
			<Setter Property="BorderBrush" Value="#FF7FFFD4" />
			<Setter Property="Margin" Value="10" />
			<Setter Property="Padding" Value="5" />
			<Setter Property="FontSize" Value="14" />
		</Style>
	</Window.Resources>

	<StackPanel Orientation="Vertical">
		<TextBlock
			Margin="20,20,20,0"
			FontSize="16"
			Text="Describe who you are, what type of events would you like to organize, ..." />
		<TextBox
			Height="300"
			Margin="20,10"
			AcceptsReturn="True"
			FontSize="14"
			Text="{Binding RequestText}"
			TextWrapping="Wrap"
			VerticalScrollBarVisibility="Auto" />
		<Button
			Width="150"
			Margin="20,10"
			Command="{Binding SendRequestCommand}"
			Content="Send request" />
	</StackPanel>
</Window>