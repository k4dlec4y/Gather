<Window
	x:Class="WPF.Views.Organizer.MainView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:views="clr-namespace:WPF.Views.Organizer"
	xmlns:vm="clr-namespace:WPF.Viewmodels.Organizer"
	Title="Organizer MainView"
	Width="1600"
	Height="800"
	Background="#FF000000"
	WindowStartupLocation="CenterScreen"
	mc:Ignorable="d">

	<Window.Resources>
		<DataTemplate DataType="{x:Type vm:MyEventsPageViewModel}">
			<views:MyEventsPageView />
		</DataTemplate>

		<DataTemplate DataType="{x:Type vm:SettingsPageViewModel}">
			<views:SettingsPageView />
		</DataTemplate>

		<Style TargetType="TextBlock">
			<Setter Property="Foreground" Value="White" />
			<Setter Property="FontSize" Value="14" />
			<Setter Property="Margin" Value="0,5,0,5" />
		</Style>

		<Style x:Key="CustomTextBoxStyle" TargetType="TextBox">
			<Setter Property="Background" Value="#FFFFFFFF" />
			<Setter Property="Foreground" Value="White" />
			<Setter Property="BorderBrush" Value="#FF7FFFD4" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="Height" Value="30" />
			<Setter Property="Margin" Value="0,0,0,15" />
			<Setter Property="Padding" Value="5" />
		</Style>

		<Style TargetType="Button">
			<Setter Property="Background" Value="#FFFF00FF" />
			<Setter Property="Foreground" Value="#FF1E1E2F" />
			<Setter Property="FontSize" Value="16" />
			<Setter Property="FontWeight" Value="Bold" />
			<Setter Property="Margin" Value="10,5" />
			<Setter Property="Padding" Value="10,5" />
			<Setter Property="BorderBrush" Value="Transparent" />
			<Setter Property="Cursor" Value="Hand" />
			<Setter Property="Effect">
				<Setter.Value>
					<DropShadowEffect
						BlurRadius="8"
						Opacity="0.6"
						ShadowDepth="0"
						Color="#FFFF00FF" />
				</Setter.Value>
			</Setter>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#FFFF00FF" />
					<Setter Property="Effect">
						<Setter.Value>
							<DropShadowEffect
								BlurRadius="15"
								ShadowDepth="0"
								Color="#FFFF00FF" />
						</Setter.Value>
					</Setter>
				</Trigger>
			</Style.Triggers>
		</Style>
	</Window.Resources>

	<i:Interaction.Triggers>
		<i:EventTrigger EventName="Closing">
			<i:InvokeCommandAction Command="{Binding SignOutCommand}" />
		</i:EventTrigger>
	</i:Interaction.Triggers>

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="220" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>

		<Grid Grid.Column="0" Background="#FF000000">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<TextBlock
				Grid.Row="0"
				Margin="0,20,0,10"
				HorizontalAlignment="Center"
				FontSize="28"
				FontWeight="Bold"
				Text="GATHER">
				<TextBlock.Foreground>
					<LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
						<GradientStop Offset="0.3" Color="#FFFF00FF" />
						<GradientStop Offset="0.7" Color="#FF7FFFD4" />
					</LinearGradientBrush>
				</TextBlock.Foreground>
				<TextBlock.Effect>
					<DropShadowEffect
						BlurRadius="12"
						Opacity="0.8"
						ShadowDepth="0"
						Color="#FFFF00FF" />
				</TextBlock.Effect>
			</TextBlock>

			<StackPanel Grid.Row="1">
				<Button
					x:Name="MyEvents"
					Command="{Binding MyEventsCommand}"
					Content="My Events" />
				<Button Command="{Binding SettingsCommand}" Content="Settings" />
			</StackPanel>

			<Label
				Grid.Row="3"
				Width="200"
				Margin="0,10"
				HorizontalAlignment="Center"
				HorizontalContentAlignment="Center"
				Background="Black"
				BorderBrush="#FFFF00FF"
				BorderThickness="1">
				<TextBlock
					Padding="5,0,5,0"
					HorizontalAlignment="Stretch"
					FontSize="16"
					Foreground="#FFFF00FF"
					Text="{Binding OrganizerIdentityService.CurrentEventOrganizer.Username, StringFormat=logged in as {0}}"
					TextAlignment="Center"
					TextWrapping="Wrap" />
			</Label>
		</Grid>

		<ContentControl
			Grid.Column="1"
			Background="#FF0a0a0a"
			Content="{Binding Navigation.CurrentPage}" />
	</Grid>
</Window>